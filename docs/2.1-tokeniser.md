# 2.1 BPE tokeniser (`src/tokenizer.py`)

This script trains a **Byte-Pair Encoding (BPE)** subword tokeniser on the **training split only**, encodes all three splits, pads sequences to a fixed length, and saves NumPy tensors plus label encodings for the neural authorship model (CNN-LSTM).

## Purpose

isiZulu is agglutinative: meaningful style and morphology often live in prefixes and suffixes (e.g. `uku-`, `ama-`, `nga-`) rather than in whole-word tokens alone. BPE learns frequent character merges from the corpus so the downstream model can represent subword patterns without a hand-built morphological analyser.

Training the tokeniser **only on train** avoids vocabulary leakage from validation and test articles.

## Prerequisites

Run [`src/preprocessing.py`](2.2-preprocessing.md) first so these files exist:

- `data/splits/train.csv`
- `data/splits/val.csv`
- `data/splits/test.csv`

## Hyperparameters

| Parameter | Value | Rationale |
|-----------|-------|-----------|
| `VOCAB_SIZE` | 3000 | Small enough for a ~184-article train set; limits memorising full words |
| `MIN_FREQUENCY` | 2 | Drop hapax merges from training |
| `PERCENTILE` | 95 | Set max sequence length from 95th percentile of **train** lengths (limits padding from outliers) |
| `RANDOM_STATE` | 42 | Label encoding reproducibility |

Special tokens: `[UNK]` (unknown subwords), `[PAD]` (padding). Pre-tokeniser: **Whitespace** (word boundaries preserved before BPE merges).

## Pipeline overview

```
train.csv ──► train BPE ──► tokenizer.json
                │
                ├── encode train / val / test
                ├── choose max_seq_len (95th pct on train)
                ├── pad / truncate → X_train, X_val, X_test
                ├── LabelEncoder on author → y_train, y_val, y_test
                └── evaluate + save metadata
```

## Main functions

| Function | Role |
|----------|------|
| `train_bpe_tokenizer` | Fits BPE on training texts only via Hugging Face `tokenizers` |
| `encode_split` | Maps each article to a variable-length list of token IDs |
| `analyse_lengths` | Computes length stats; sets `max_sequence_length`; saves histogram |
| `pad_and_truncate` | Builds fixed-size `(n_samples, max_sequence_length)` arrays; pads with `[PAD]` id (not 0) |
| `encode_labels` | `sklearn.preprocessing.LabelEncoder` fit on train authors |
| `inspect_bpe_segmentations` | Prints example subword splits for manual QA |
| `evaluate_tokenizer` | UNK rate, fertility, truncation rate, vocabulary coverage |
| `save_arrays` | Writes `.npy` files, `label_classes.json`, `tokenizer_metadata.json` |

### Padding detail

Arrays are pre-filled with the padding token ID (`1` for `[PAD]` in the current run). Sequences longer than `max_sequence_length` are truncated from the start (first `max_sequence_length` tokens kept). The CNN-LSTM masking layer must treat this pad id as non-content, not zero.

## Outputs

| File | Description |
|------|-------------|
| `outputs/bpe_tokenizer/tokenizer.json` | Serialised BPE model |
| `outputs/figures/sequence_length_histogram.png` | Train length distribution with 95th-percentile cutoff |
| `data/splits/X_train.npy` | Padded token IDs, shape `(184, 1012)` |
| `data/splits/X_val.npy` | Shape `(39, 1012)` |
| `data/splits/X_test.npy` | Shape `(40, 1012)` |
| `data/splits/y_train.npy` | Integer labels 0–10 |
| `data/splits/y_val.npy` | Validation labels |
| `data/splits/y_test.npy` | Test labels |
| `data/splits/label_classes.json` | Author names in label order |
| `outputs/metrics/tokenizer_metadata.json` | Full run configuration and evaluation metrics |

### Latest run metrics (`tokenizer_metadata.json`)

| Metric | Value |
|--------|-------|
| `max_sequence_length` | 1012 (95th percentile of train lengths) |
| UNK rate | 0.0012% |
| Fertility | 2.34 subwords / word |
| Truncation rate | 4.56% of articles |
| Vocabulary coverage | 96.63% of vocab used |
| Classes | 11 authors |

Label order in `label_classes.json` (alphabetical by author name):

Charles Khuzwayo, Fanelesibonge Bengu, Mhlengi Shangase, Mthokozisi Mncuseni, Nokubongwa Phenyane, Nonkululeko Nhlapo, Sabelo Nsele, Sibusiso Mdlalose, Simangaliso Ntshangase, Zakhele Xaba, Zimbili Vilakazi.

## Usage

From the project root:

```bash
python3 src/tokenizer.py
```

## Dependencies

- `tokenizers` (Hugging Face)
- `numpy`, `pandas`, `scikit-learn`, `matplotlib`

Listed in `requirements.txt`.

## Design choices

1. **Train-only BPE** — Same principle as fitting TF-IDF or embeddings only on train: test vocabulary must not influence token merge statistics.
2. **Percentile-based max length** — Using the absolute maximum length would force very long padding for a few outlier articles; the 95th percentile balances truncation vs. efficiency.
3. **Small vocabulary (3000)** — On a low-resource corpus, a large vocab encourages sparsity and overfitting to surface forms.

## Next step

Use `X_*.npy` and `y_*.npy` for the CNN-LSTM model. Compare against [`src/baselines.py`](2.3-baselines.md), which benchmarks on raw text from the CSV splits (independent of BPE).
